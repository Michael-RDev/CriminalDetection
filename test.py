arr = [(),(8, 91, 116, 0)]

for indx,array in enumerate(arr):
    if arr[indx]:
        print(arr[indx])
        for value in arr[indx]:
            values = [value, value, value, value]
            
